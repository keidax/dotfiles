# Perform cd on a plain directory command
setopt auto_cd

# cd commands act like pushd
setopt auto_pushd

# Turn on autocorrect
setopt correct

# What it sounds like
setopt interactive_comments

# Show PID in job status
setopt long_list_jobs

# Report job status immediately
# setopt notify
