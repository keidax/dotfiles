- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.vim: vim
    ~/.vimrc: vimrc
    ~/.bashrc: bashrc
    ~/.bash_aliases: bash_aliases

- shell:
      - [git submodule update --init --recursive, Installing submodules]
