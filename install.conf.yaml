- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.vim: vim
    ~/.vimrc: vimrc
    ~/.bashrc: bashrc
    ~/.profile: profile
    ~/.dircolors: dircolors
    ~/.gitconfig: gitconfig

- shell:
      - [git submodule update --init --recursive, Installing submodules]
